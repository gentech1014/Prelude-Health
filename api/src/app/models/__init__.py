"""Domain models: the internal shape of a session, its transcript, and its report.

Distinct from `app.schemas`, which defines what the API accepts/returns —
domain models are persistence- and agent-facing, schemas are wire-facing.
"""
