// Shared semantic tone used across state/feedback components — maps 1:1 to the
// fixed clinical semantic colors in theme.css. Never extend with brand colors (gold/plum).
export type Tone = 'neutral' | 'success' | 'warning' | 'error' | 'info';
